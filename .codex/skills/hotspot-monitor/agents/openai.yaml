interface:
  display_name: "Hotspot Monitor"
  short_description: "Monitor keywords and return credible hotspot events"
  brand_color: "#0891B2"
  default_prompt: "Use $hotspot-monitor to scan recent credible hotspots for my keywords and summarize trusted events."

policy:
  allow_implicit_invocation: true

